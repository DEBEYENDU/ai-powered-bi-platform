# Placeholder Secrets. In production use Sealed Secrets or External Secrets
# (AWS Secrets Manager / Azure Key Vault / GCP Secret Manager) — see
# infra/terraform. NEVER commit real values here.
apiVersion: v1
kind: Secret
metadata:
  name: bi-secrets
  namespace: bi-platform
type: Opaque
stringData:
  POSTGRES_PASSWORD: CHANGEME-postgres
  REDIS_PASSWORD: CHANGEME-redis
  JWT_SECRET_KEY: CHANGEME-jwt-min-32-chars-long-secret
  GRAFANA_PASSWORD: CHANGEME-grafana
